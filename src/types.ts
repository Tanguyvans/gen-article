export interface ProjectSettings {
  wordpress_url: string;
  wordpress_user: string;
  wordpress_pass: string;
  // Add the new prompt fields
  general_prompt: string;
  section_instructions: string[];
} 